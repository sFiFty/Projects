(function (){    const btn = document.querySelector(".generateJoke");    const out = document.querySelector(".joke");    let clicked = false;    showPreloader();    btn.addEventListener("click", generate);    function generate(){        if (clicked) return;        clicked = true;        showPreloader();        fetch("https://icanhazdadjoke.com/", {            headers: {                "Accept": "application/json"            }        })            .then(e => e.json())            .then(e => {                setTimeout(() => {                    out.textContent = e.joke;                    clicked = false;                }, 200)            });    }    generate();    function showPreloader(){        out.innerHTML = `<img src="./imgs/joke/Preloader_7.gif">`;        let img = out.children[0];        img.style.display = "block";        img.style.margin = "0 auto";    }})();